# 图文生成小程序 - 后台管理系统设计文档

> 版本：v1.0  
> 更新日期：2026-01-25  
> 项目定位：为图文生成小程序提供完整的后台管理能力，支持分类管理、作品管理、用户管理、数据面板等核心功能。

---

## 📋 目录

1. [技术栈选型](#1-技术栈选型)
2. [UI 设计风格](#2-ui-设计风格)
3. [项目结构](#3-项目结构)
4. [功能模块详解](#4-功能模块详解)
5. [API 接口规划](#5-api-接口规划)
6. [开发计划](#6-开发计划)
7. [构建与打包配置](#7-构建与打包配置)
8. [性能优化与监控](#8-性能优化与监控)
9. [安全与隐私保护](#9-安全与隐私保护)
10. [测试与发布流程](#10-测试与发布流程)

---

## 1. 技术栈选型

### 1.1 前端核心框架

| 技术 | 版本 | 说明 |
|------|------|------|
| **Vue 3** | ^3.4.x | 渐进式 JavaScript 框架，Composition API |
| **Vite** | ^5.x | 下一代前端构建工具，极速冷启动 |
| **TypeScript** | ^5.x | 类型安全，提升代码质量 |
| **Pinia** | ^2.x | Vue 官方状态管理库 |
| **Vue Router** | ^4.x | 官方路由管理器 |

### 1.2 UI 组件库

| 组件库 | 说明 |
|--------|------|
| **Naive UI** | 主力 UI 组件库，圆角友好、主题定制灵活、TypeScript 支持完善 |
| **@vicons/ionicons5** | 图标库，配合 Naive UI 使用 |

### 1.3 工具库

| 库名 | 用途 |
|------|------|
| **Axios** | HTTP 请求封装 |
| **Zod** | 运行时类型验证与 Schema 定义 |
| **dayjs** | 日期处理 |
| **echarts** | 数据可视化图表 |
| **@vueuse/core** | Vue 组合式 API 工具集 |
| **unplugin-auto-import** | 自动导入 |
| **unplugin-vue-components** | 组件自动注册 |

### 1.4 类型验证（Zod）

使用 Zod 进行运行时类型验证，替代传统的 TypeScript interface 定义：

```typescript
import { z } from 'zod';

// 定义 Schema
export const CategorySchema = z.object({
  id: z.number(),
  name: z.string().min(1, '分类名称不能为空'),
  code: z.string().regex(/^[a-z0-9]+$/, '编码只能包含小写字母和数字'),
  type: z.enum(['style', 'model']),
  icon: z.string().optional(),
  cover: z.string().url().optional(),
  description: z.string().max(200).optional(),
  parentId: z.number().optional(),
  sort: z.number().int().min(0),
  status: z.union([z.literal(0), z.literal(1)]),
  createdAt: z.string().datetime(),
  updatedAt: z.string().datetime(),
});

// 自动推导 TypeScript 类型
export type Category = z.infer<typeof CategorySchema>;

// 表单验证 Schema（创建时不需要 id 和时间字段）
export const CategoryFormSchema = CategorySchema.omit({
  id: true,
  createdAt: true,
  updatedAt: true,
});

export type CategoryForm = z.infer<typeof CategoryFormSchema>;
```

**Zod 使用场景：**
- API 响应数据验证
- 表单数据验证
- 环境变量验证
- 统一类型定义与运行时检查

### 1.4 代码规范

| 工具 | 说明 |
|------|------|
| **ESLint** | 代码检查 |
| **Prettier** | 代码格式化 |
| **husky** | Git hooks |
| **lint-staged** | 暂存文件检查 |

---

## 2. UI 设计风格

### 2.1 整体风格定义

```
🎨 设计理念：现代简约 · 圆润友好 · 高效专业
```

#### 风格关键词
- **简约现代**：减少视觉噪音，突出核心内容
- **圆润柔和**：大圆角设计（8px-16px），降低视觉压迫感
- **清爽配色**：以白色为主背景，辅以品牌色点缀
- **信息层次**：通过卡片、阴影、间距建立清晰的信息层级

### 2.2 色彩系统

```css
/* 主色调 - 品牌色 */
--primary-color: #18a058;        /* 绿色主色 - Naive UI 默认 */
--primary-color-hover: #36ad6a;
--primary-color-pressed: #0c7a43;

/* 功能色 */
--success-color: #18a058;        /* 成功 */
--warning-color: #f0a020;        /* 警告 */
--error-color: #d03050;          /* 错误 */
--info-color: #2080f0;           /* 信息 */

/* 中性色 */
--text-color-primary: #333639;   /* 主要文字 */
--text-color-secondary: #666;    /* 次要文字 */
--text-color-tertiary: #999;     /* 辅助文字 */
--border-color: #e0e0e6;         /* 边框色 */
--bg-color: #f5f7f9;             /* 页面背景 */
--card-bg-color: #ffffff;        /* 卡片背景 */
```

### 2.3 布局规范

```
┌─────────────────────────────────────────────────────────────┐
│  顶部导航栏 (64px)  - Logo + 面包屑 + 用户信息 + 通知       │
├────────────┬────────────────────────────────────────────────┤
│            │                                                │
│   侧边栏   │              主内容区域                        │
│  (220px)   │                                                │
│            │   ┌────────────────────────────────────────┐   │
│  · 数据面板 │   │  页面标题 + 操作按钮区                  │   │
│  · 分类管理 │   ├────────────────────────────────────────┤   │
│  · 作品管理 │   │                                        │   │
│  · 用户管理 │   │           内容区域（卡片式）            │   │
│  · 系统设置 │   │                                        │   │
│            │   │                                        │   │
│            │   └────────────────────────────────────────┘   │
│            │                                                │
└────────────┴────────────────────────────────────────────────┘
```

### 2.4 组件风格规范

#### 按钮样式
```typescript
// 主要操作 - 填充按钮
<n-button type="primary" round>新增分类</n-button>

// 次要操作 - 边框按钮  
<n-button secondary round>批量导出</n-button>

// 危险操作 - 红色边框/填充
<n-button type="error" ghost round>删除</n-button>
```

#### 卡片样式
```css
.content-card {
  border-radius: 12px;
  padding: 20px;
  background: #fff;
  box-shadow: 0 2px 12px rgba(0, 0, 0, 0.04);
}
```

#### 表格样式
```typescript
// 使用 Naive UI 的 n-data-table
// 开启斑马纹、边框圆角
<n-data-table 
  :columns="columns" 
  :data="data"
  :bordered="false"
  striped
  :single-line="false"
/>
```

### 2.5 响应式断点

| 断点 | 宽度 | 说明 |
|------|------|------|
| xs | < 576px | 移动端（隐藏侧边栏） |
| sm | ≥ 576px | 小屏 |
| md | ≥ 768px | 平板 |
| lg | ≥ 992px | 桌面端 |
| xl | ≥ 1200px | 大屏幕 |
| xxl | ≥ 1600px | 超大屏幕 |

### 2.6 主题与明暗模式

为提升可访问性与使用舒适度，后台管理界面需提供明亮/暗黑双主题，并支持自动与手动切换：

- **主题策略**：首次加载读取 `prefers-color-scheme`，优先跟随系统；提供顶部主题切换按钮，状态持久化至本地（`localStorage` 或 `cookie`）并同步 Pinia `app` 模块。
- **配色变量**：统一抽象为主题 Token，明暗模式分别定义核心色、中性色与背景层级，保证对比度满足 WCAG 2.1 AA 要求。

```scss
/* light.theme.scss */
:root {
  --color-bg-page: #f5f7f9;
  --color-bg-card: #ffffff;
  --color-text-primary: #1f2429;
  --color-text-secondary: #5b6673;
  --color-border: #e0e5eb;
  --color-shadow: 0 2px 12px rgba(15, 23, 42, 0.06);
}

/* dark.theme.scss */
[data-theme='dark'] {
  --color-bg-page: #0f172a;
  --color-bg-card: #111c34;
  --color-text-primary: #eff4ff;
  --color-text-secondary: #c4c9d6;
  --color-border: rgba(148, 163, 184, 0.24);
  --color-shadow: 0 6px 20px rgba(15, 23, 42, 0.45);
}
```

- **组件适配**：通过 Naive UI `NConfigProvider` 注入主题变量，表格、卡片与弹窗需在暗色模式下调整背景与边框透明度，禁用高透明度阴影。
- **图表与状态色**：ECharts 配置 light/dark 双主题，暗色模式下将危险/警告色亮度提升 10%，并保持背景透明以融入深色卡片。
- **可访问性校验**：关键文本与交互控件对比度 ≥ 4.5:1，聚焦态提供可视化描边，并在 QA 阶段纳入主题切换串测及截图基线。
- **交互流程**：
  1. 读取系统主题 → 若用户手动切换，则覆盖系统偏好。
  2. 动态更新 `<html data-theme="...">`，触发 CSS 变量生效。
  3. 使用 `matchMedia` 监听系统主题变更，在未手动锁定时实时同步。

---

## 3. 项目结构

```
admin/
├── public/                      # 静态资源
│   └── favicon.ico
├── src/
│   ├── api/                     # API 接口封装
│   │   ├── index.ts            # API 统一导出
│   │   ├── request.ts          # Axios 封装
│   │   ├── category.ts         # 分类管理 API
│   │   ├── works.ts            # 作品管理 API
│   │   ├── user.ts             # 用户管理 API
│   │   └── dashboard.ts        # 数据面板 API
│   │
│   ├── assets/                  # 静态资源
│   │   ├── images/
│   │   └── styles/
│   │       ├── variables.scss  # SCSS 变量
│   │       ├── mixins.scss     # SCSS 混入
│   │       └── global.scss     # 全局样式
│   │
│   ├── components/              # 通用组件
│   │   ├── Layout/             # 布局组件
│   │   │   ├── Sidebar.vue     # 侧边栏
│   │   │   ├── Header.vue      # 顶部导航
│   │   │   └── MainLayout.vue  # 主布局
│   │   ├── Common/             # 通用组件
│   │   │   ├── PageHeader.vue  # 页面标题
│   │   │   ├── SearchBar.vue   # 搜索栏
│   │   │   ├── ImagePreview.vue# 图片预览
│   │   │   └── ConfirmModal.vue# 确认弹窗
│   │   └── Charts/             # 图表组件
│   │       ├── LineChart.vue
│   │       ├── PieChart.vue
│   │       └── BarChart.vue
│   │
│   ├── composables/             # 组合式函数
│   │   ├── useTable.ts         # 表格通用逻辑
│   │   ├── useModal.ts         # 弹窗通用逻辑
│   │   └── useAuth.ts          # 权限相关
│   │
│   ├── router/                  # 路由配置
│   │   ├── index.ts
│   │   ├── routes.ts           # 路由表
│   │   └── guard.ts            # 路由守卫
│   │
│   ├── store/                   # Pinia 状态管理
│   │   ├── index.ts
│   │   ├── modules/
│   │   │   ├── user.ts         # 用户状态
│   │   │   ├── app.ts          # 应用状态
│   │   │   └── permission.ts   # 权限状态
│   │
│   ├── views/                   # 页面视图
│   │   ├── login/              # 登录页
│   │   │   └── index.vue
│   │   ├── dashboard/          # 数据面板
│   │   │   └── index.vue
│   │   ├── category/           # 分类管理
│   │   │   ├── index.vue       # 列表页
│   │   │   └── components/     # 页面组件
│   │   │       └── CategoryForm.vue
│   │   ├── works/              # 作品管理
│   │   │   ├── index.vue       # 列表页
│   │   │   ├── detail.vue      # 详情页
│   │   │   └── components/
│   │   ├── user/               # 用户管理
│   │   │   ├── index.vue
│   │   │   └── components/
│   │   └── system/             # 系统设置
│   │       ├── config.vue
│   │       └── log.vue
│   │
│   ├── utils/                   # 工具函数
│   │   ├── index.ts
│   │   ├── storage.ts          # 本地存储
│   │   ├── format.ts           # 格式化
│   │   └── validate.ts         # 验证
│   │
│   ├── types/                   # TypeScript 类型
│   │   ├── api.d.ts
│   │   ├── category.d.ts
│   │   ├── works.d.ts
│   │   └── user.d.ts
│   │
│   ├── App.vue                  # 根组件
│   ├── main.ts                  # 入口文件
│   └── env.d.ts                 # 环境变量类型
│
├── .env                         # 环境变量
├── .env.development
├── .env.production
├── .eslintrc.cjs
├── .prettierrc
├── index.html
├── package.json
├── tsconfig.json
├── vite.config.ts
└── README.md
```

---

## 4. 功能模块详解

### 4.1 数据面板 (Dashboard)

#### 功能概述
数据面板是管理系统的首页，提供系统核心数据的可视化展示。

#### 功能清单
| 功能 | 说明 |
|------|------|
| 统计卡片 | 今日生成数、总作品数、用户数、收入统计 |
| 趋势图表 | 近7天/30天生成趋势折线图 |
| 风格分布 | 各风格使用占比饼图 |
| 模型调用 | 各模型调用次数柱状图 |
| 最新作品 | 最近生成的作品列表 |
| 活跃用户 | 最活跃用户 TOP10 |

#### 页面布局示意
```
┌─────────────────────────────────────────────────────────────┐
│  📊 数据面板                                    刷新 | 导出  │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐           │
│  │ 今日生成 │ │ 总作品数 │ │ 用户总数 │ │ 今日收入 │           │
│  │  1,234  │ │ 56,789  │ │  8,901  │ │ ¥3,456  │           │
│  │  ↑12%   │ │  ↑5%    │ │  ↑8%    │ │  ↑15%   │           │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘           │
│                                                             │
│  ┌──────────────────────────────┐ ┌────────────────────┐   │
│  │                              │ │                    │   │
│  │     生成趋势图 (折线)         │ │   风格分布 (饼图)   │   │
│  │                              │ │                    │   │
│  └──────────────────────────────┘ └────────────────────┘   │
│                                                             │
│  ┌──────────────────────────────┐ ┌────────────────────┐   │
│  │                              │ │                    │   │
│  │     模型调用统计 (柱状)       │ │   最新作品列表      │   │
│  │                              │ │                    │   │
│  └──────────────────────────────┘ └────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

### 4.2 分类管理 (Category)

#### 功能概述
管理风格分类、模型分类等系统分类数据。

#### 功能清单
| 功能 | 说明 |
|------|------|
| 分类列表 | 树形/平铺展示所有分类 |
| 新增分类 | 添加新分类（名称、图标、排序、状态） |
| 编辑分类 | 修改分类信息 |
| 删除分类 | 删除分类（检查关联作品） |
| 排序调整 | 拖拽排序 |
| 状态切换 | 启用/禁用分类 |

#### 数据结构（Zod Schema）
```typescript
import { z } from 'zod';

// 分类 Schema
export const CategorySchema = z.object({
  id: z.number(),
  name: z.string().min(1, '分类名称不能为空').max(50, '分类名称不超过50字'),
  code: z.string().regex(/^[a-z0-9_]+$/, '编码只能包含小写字母、数字和下划线'),
  type: z.enum(['style', 'model'], {
    errorMap: () => ({ message: '分类类型必须是 style 或 model' })
  }),
  icon: z.string().optional(),
  cover: z.string().url('封面图必须是有效URL').optional(),
  description: z.string().max(200, '描述不超过200字').optional(),
  parentId: z.number().int().positive().optional(),
  sort: z.number().int().min(0, '排序值不能为负'),
  status: z.union([z.literal(0), z.literal(1)]),  // 0-禁用 1-启用
  createdAt: z.string().datetime(),
  updatedAt: z.string().datetime(),
});

// 类型推导
export type Category = z.infer<typeof CategorySchema>;

// 创建表单 Schema（排除自动生成字段）
export const CategoryCreateSchema = CategorySchema.omit({
  id: true,
  createdAt: true,
  updatedAt: true,
});
export type CategoryCreate = z.infer<typeof CategoryCreateSchema>;

// 更新表单 Schema（所有字段可选，除了 id）
export const CategoryUpdateSchema = CategorySchema.partial().required({ id: true });
export type CategoryUpdate = z.infer<typeof CategoryUpdateSchema>;
```

#### 页面布局示意
```
┌─────────────────────────────────────────────────────────────┐
│  📁 分类管理                                    + 新增分类   │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌────────────────────────────────────────────────────┐    │
│  │  🔍 搜索分类名称    [类型▼] [状态▼]      搜索 | 重置 │    │
│  └────────────────────────────────────────────────────┘    │
│                                                             │
│  ┌────────────────────────────────────────────────────┐    │
│  │ ☐ │ 分类名称     │ 编码   │ 类型  │ 状态 │ 操作    │    │
│  ├───┼─────────────┼────────┼───────┼──────┼─────────┤    │
│  │ ☐ │ 🎨 国风      │ gf001  │ 风格  │ ✓启用 │ 编辑 删除│    │
│  │ ☐ │ 🤖 赛博朋克  │ cb002  │ 风格  │ ✓启用 │ 编辑 删除│    │
│  │ ☐ │ 🌸 日漫      │ rm003  │ 风格  │ ✓启用 │ 编辑 删除│    │
│  │ ☐ │ 📷 写实      │ xs004  │ 风格  │ ○禁用 │ 编辑 删除│    │
│  └────────────────────────────────────────────────────┘    │
│                                                             │
│  共 24 条  < 1 2 3 4 5 >                                    │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

### 4.3 作品管理 (Works)

#### 功能概述
管理所有用户生成的作品，支持查看、审核、删除等操作。

#### 功能清单
| 功能 | 说明 |
|------|------|
| 作品列表 | 分页展示所有作品 |
| 作品详情 | 查看作品完整信息 |
| 作品审核 | 内容审核（通过/拒绝） |
| 作品删除 | 删除违规/无效作品 |
| 高级筛选 | 按风格、模型、用户、时间等筛选 |
| 批量操作 | 批量审核、批量删除 |
| 数据导出 | 导出作品数据 |

#### 数据结构（Zod Schema）
```typescript
import { z } from 'zod';

// 生成参数 Schema
export const WorkParamsSchema = z.object({
  seed: z.number().int(),
  steps: z.number().int().min(1).max(150),
  guidance: z.number().min(0).max(30),
});

// 审核状态枚举
export const WorkStatusEnum = z.enum(['pending', 'approved', 'rejected']);
export type WorkStatus = z.infer<typeof WorkStatusEnum>;

// 作品 Schema
export const WorkSchema = z.object({
  id: z.number(),
  userId: z.number(),
  userName: z.string(),
  prompt: z.string().min(1, '提示词不能为空').max(2000),
  negativePrompt: z.string().max(1000).optional(),
  styleId: z.number(),
  styleName: z.string(),
  modelId: z.number(),
  modelName: z.string(),
  imageUrl: z.string().url('图片地址必须是有效URL'),
  thumbnailUrl: z.string().url('缩略图地址必须是有效URL'),
  width: z.number().int().positive(),
  height: z.number().int().positive(),
  params: WorkParamsSchema,
  status: WorkStatusEnum,
  isPublic: z.boolean(),
  viewCount: z.number().int().min(0),
  downloadCount: z.number().int().min(0),
  createdAt: z.string().datetime(),
  updatedAt: z.string().datetime(),
});

// 类型推导
export type Work = z.infer<typeof WorkSchema>;
export type WorkParams = z.infer<typeof WorkParamsSchema>;

// 作品审核 Schema
export const WorkAuditSchema = z.object({
  id: z.number(),
  status: WorkStatusEnum,
  reason: z.string().max(500).optional(),  // 拒绝原因
});
export type WorkAudit = z.infer<typeof WorkAuditSchema>;

// 批量审核 Schema
export const WorkBatchAuditSchema = z.object({
  ids: z.array(z.number()).min(1, '至少选择一个作品'),
  status: WorkStatusEnum,
  reason: z.string().max(500).optional(),
});
export type WorkBatchAudit = z.infer<typeof WorkBatchAuditSchema>;
```

#### 页面布局示意
```
┌─────────────────────────────────────────────────────────────┐
│  🖼️ 作品管理                          批量审核 | 批量删除   │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌────────────────────────────────────────────────────┐    │
│  │ 🔍 搜索提示词/用户  [风格▼] [模型▼] [状态▼] [日期]  │    │
│  │                                         搜索 | 重置 │    │
│  └────────────────────────────────────────────────────┘    │
│                                                             │
│  📋 列表视图  |  🖼️ 卡片视图                                │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐   │   │
│  │ │     │ │     │ │     │ │     │ │     │ │     │   │   │
│  │ │ 图片 │ │ 图片 │ │ 图片 │ │ 图片 │ │ 图片 │ │ 图片 │   │   │
│  │ │     │ │     │ │     │ │     │ │     │ │     │   │   │
│  │ ├─────┤ ├─────┤ ├─────┤ ├─────┤ ├─────┤ ├─────┤   │   │
│  │ │ 国风 │ │ 赛博 │ │ 日漫 │ │ 写实 │ │ 插画 │ │ 极简 │   │   │
│  │ │ 张三 │ │ 李四 │ │ 王五 │ │ 赵六 │ │ 钱七 │ │ 孙八 │   │   │
│  │ │✓待审│ │✓通过│ │✗拒绝│ │✓通过│ │✓待审│ │✓通过│   │   │
│  │ └─────┘ └─────┘ └─────┘ └─────┘ └─────┘ └─────┘   │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  共 1,234 条  < 1 2 3 ... 99 >                              │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

### 4.4 用户管理 (User)

#### 功能概述
管理系统用户，包括普通用户和管理员。

#### 功能清单
| 功能 | 说明 |
|------|------|
| 用户列表 | 分页展示所有用户 |
| 用户详情 | 查看用户完整信息及作品 |
| 用户封禁 | 封禁/解封用户 |
| 会员管理 | 查看/调整用户会员状态 |
| 额度调整 | 手动调整用户生成额度 |
| 高级筛选 | 按状态、会员、注册时间等筛选 |
| 数据导出 | 导出用户数据 |

#### 数据结构（Zod Schema）
```typescript
import { z } from 'zod';

// 性别枚举
export const GenderEnum = z.union([z.literal(0), z.literal(1), z.literal(2)]);
export type Gender = z.infer<typeof GenderEnum>;  // 0-未知 1-男 2-女

// 会员类型枚举
export const MemberTypeEnum = z.enum(['free', 'basic', 'pro']);
export type MemberType = z.infer<typeof MemberTypeEnum>;

// 用户状态枚举
export const UserStatusEnum = z.union([z.literal(0), z.literal(1)]);
export type UserStatus = z.infer<typeof UserStatusEnum>;  // 0-封禁 1-正常

// 用户 Schema
export const UserSchema = z.object({
  id: z.number(),
  openid: z.string(),
  unionid: z.string().optional(),
  nickname: z.string().min(1).max(50),
  avatar: z.string().url(),
  phone: z.string().regex(/^1[3-9]\d{9}$/, '手机号格式不正确').optional(),
  gender: GenderEnum,
  
  // 会员信息
  memberType: MemberTypeEnum,
  memberExpireAt: z.string().datetime().optional(),
  
  // 额度信息
  totalQuota: z.number().int().min(0),
  usedQuota: z.number().int().min(0),
  remainQuota: z.number().int().min(0),
  
  // 统计信息
  worksCount: z.number().int().min(0),
  totalGenerate: z.number().int().min(0),
  
  // 状态
  status: UserStatusEnum,
  
  // 时间
  lastLoginAt: z.string().datetime(),
  createdAt: z.string().datetime(),
  updatedAt: z.string().datetime(),
});

// 类型推导
export type User = z.infer<typeof UserSchema>;

// 用户封禁/解封 Schema
export const UserStatusUpdateSchema = z.object({
  id: z.number(),
  status: UserStatusEnum,
  reason: z.string().max(200).optional(),  // 封禁原因
});
export type UserStatusUpdate = z.infer<typeof UserStatusUpdateSchema>;

// 用户额度调整 Schema
export const UserQuotaUpdateSchema = z.object({
  id: z.number(),
  quota: z.number().int(),  // 正数增加，负数减少
  reason: z.string().min(1, '请填写调整原因').max(200),
});
export type UserQuotaUpdate = z.infer<typeof UserQuotaUpdateSchema>;

// 用户会员调整 Schema
export const UserMemberUpdateSchema = z.object({
  id: z.number(),
  memberType: MemberTypeEnum,
  expireAt: z.string().datetime().optional(),
  reason: z.string().max(200).optional(),
});
export type UserMemberUpdate = z.infer<typeof UserMemberUpdateSchema>;
```

#### 页面布局示意
```
┌─────────────────────────────────────────────────────────────┐
│  👥 用户管理                                      导出用户   │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌────────────────────────────────────────────────────┐    │
│  │ 🔍 搜索昵称/手机号  [会员▼] [状态▼] [注册时间]       │    │
│  │                                         搜索 | 重置 │    │
│  └────────────────────────────────────────────────────┘    │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ ☐ │ 用户信息      │ 会员  │ 额度    │ 作品 │ 状态│操作│   │
│  ├───┼──────────────┼───────┼─────────┼─────┼────┼────┤   │
│  │ ☐ │ 🧑 张三       │ Pro   │ 80/100  │ 56  │ 正常│详情│   │
│  │   │ 138****1234  │ 2026-02│         │     │    │封禁│   │
│  ├───┼──────────────┼───────┼─────────┼─────┼────┼────┤   │
│  │ ☐ │ 👩 李四       │ Basic │ 15/30   │ 23  │ 正常│详情│   │
│  │   │ 139****5678  │ 2026-03│         │     │    │封禁│   │
│  ├───┼──────────────┼───────┼─────────┼─────┼────┼────┤   │
│  │ ☐ │ 🧑 王五       │ Free  │ 2/5     │ 8   │ 封禁│详情│   │
│  │   │ 未绑定       │ -     │         │     │    │解封│   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  共 8,901 条  < 1 2 3 ... 445 >                             │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 5. API 接口规划

### 5.1 接口规范

#### 请求格式
```typescript
// 统一请求头
{
  'Content-Type': 'application/json',
  'Authorization': 'Bearer <token>'
}
```

#### 响应格式
```typescript
// 统一响应结构
interface ApiResponse<T> {
  code: number;        // 状态码：0-成功，其他-失败
  message: string;     // 提示信息
  data: T;             // 响应数据
}

// 分页响应
interface PageResponse<T> {
  list: T[];           // 数据列表
  total: number;       // 总条数
  page: number;        // 当前页
  pageSize: number;    // 每页条数
}
```

### 5.2 接口清单

#### 认证模块
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/admin/login` | 管理员登录 |
| POST | `/api/admin/logout` | 退出登录 |
| GET | `/api/admin/profile` | 获取当前用户信息 |
| PUT | `/api/admin/password` | 修改密码 |

#### 数据面板
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/admin/dashboard/overview` | 概览数据 |
| GET | `/api/admin/dashboard/trend` | 趋势数据 |
| GET | `/api/admin/dashboard/style-dist` | 风格分布 |
| GET | `/api/admin/dashboard/model-stat` | 模型统计 |

#### 分类管理
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/admin/category` | 分类列表 |
| POST | `/api/admin/category` | 新增分类 |
| PUT | `/api/admin/category/:id` | 编辑分类 |
| DELETE | `/api/admin/category/:id` | 删除分类 |
| PUT | `/api/admin/category/:id/status` | 更新状态 |
| PUT | `/api/admin/category/sort` | 批量排序 |

#### 作品管理
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/admin/works` | 作品列表 |
| GET | `/api/admin/works/:id` | 作品详情 |
| PUT | `/api/admin/works/:id/audit` | 审核作品 |
| DELETE | `/api/admin/works/:id` | 删除作品 |
| POST | `/api/admin/works/batch-audit` | 批量审核 |
| POST | `/api/admin/works/batch-delete` | 批量删除 |

#### 用户管理
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/admin/user` | 用户列表 |
| GET | `/api/admin/user/:id` | 用户详情 |
| PUT | `/api/admin/user/:id/status` | 封禁/解封 |
| PUT | `/api/admin/user/:id/quota` | 调整额度 |
| PUT | `/api/admin/user/:id/member` | 调整会员 |
| GET | `/api/admin/user/:id/works` | 用户作品 |

---

## 6. 开发计划

### 6.1 阶段规划

#### Phase 1：基础框架搭建（2天）
- [x] 项目初始化（Vite + Vue3 + TS）
- [ ] 配置 ESLint + Prettier
- [ ] 集成 Naive UI
- [ ] 路由配置
- [ ] 布局组件开发
- [ ] API 请求封装
- [ ] 登录页面

#### Phase 2：数据面板（1天）
- [ ] 统计卡片组件
- [ ] ECharts 图表集成
- [ ] 趋势图表
- [ ] 分布图表
- [ ] 实时数据刷新

#### Phase 3：分类管理（1天）
- [ ] 分类列表页
- [ ] 新增/编辑表单
- [ ] 删除确认
- [ ] 拖拽排序
- [ ] 状态切换

#### Phase 4：作品管理（1.5天）
- [ ] 作品列表页
- [ ] 列表/卡片视图切换
- [ ] 作品详情弹窗
- [ ] 审核功能
- [ ] 批量操作
- [ ] 高级筛选

#### Phase 5：用户管理（1.5天）
- [ ] 用户列表页
- [ ] 用户详情页
- [ ] 封禁/解封
- [ ] 额度调整
- [ ] 用户作品查看

#### Phase 6：优化与测试（1天）
- [ ] 响应式适配
- [ ] 性能优化
- [ ] 错误处理
- [ ] 功能测试
- [ ] 主题明暗模式适配验证

### 6.2 启动命令

```bash
# 安装依赖
npm install

# 开发模式
npm run dev

# 构建生产版本
npm run build

# 预览生产版本
npm run preview

# 代码检查
npm run lint
```

---

## 7. 构建与打包配置

### 7.1 Vite 构建基线
- 统一构建目标为 `es2018`，生产模式禁用源码映射，预览/调试模式开启 `sourcemap` 便于定位问题。
- 引入 `splitVendorChunkPlugin` 拆分第三方依赖，主包锁定在 300KB 以内，Business 代码按路由懒加载。
- 打包输出目录固定为 `dist`，通过环境变量 `VITE_PUBLIC_PATH` 控制部署子路径。

```ts
import { defineConfig, splitVendorChunkPlugin } from 'vite';
import vue from '@vitejs/plugin-vue';

export default defineConfig(({ mode }) => ({
  plugins: [vue(), splitVendorChunkPlugin()],
  base: mode === 'production' ? process.env.VITE_PUBLIC_PATH ?? '/admin/' : '/',
  build: {
    outDir: 'dist',
    target: 'es2018',
    sourcemap: mode !== 'production',
    cssCodeSplit: true,
    chunkSizeWarningLimit: 1200,
    rollupOptions: {
      output: {
        manualChunks: {
          vue: ['vue', 'vue-router', 'pinia'],
          ui: ['naive-ui', '@vicons/ionicons5'],
          echarts: ['echarts']
        }
      }
    }
  }
}));
```

### 7.2 环境变量与配置分离
- `.env`：公共变量，例如 `VITE_APP_NAME`、`VITE_API_BASE`。
- `.env.development`：开发环境使用内网 API，启用 `https=true`。
- `.env.production`：生产环境开启 `VITE_REPORT_GZIP=true` 与 `VITE_PUBLIC_PATH=/admin/`。
- 通过 `import.meta.env.MODE` 决定日志等级与调试功能，确保打包产物不包含调试用依赖。

### 7.3 静态资源与 CDN 策略
- 图片、图标上传至对象存储，Vite 构建仅保留必须的占位资源，开启 `assetsInlineLimit=4096`。
- 产物发布至 CDN 时同步生成 `manifest.json`，供后端模板引用；设置长缓存 `Cache-Control: public,max-age=31536000,immutable`。
- 需要回滚时，版本号写入 `package.json version` + `commit hash`，用于静态资源目录命名。

### 7.4 部署前置校验
- `npm run lint && npm run typecheck` 通过后方可进入构建步骤。
- 构建产物上传前执行 `npm run build -- --emptyOutDir`，避免旧文件残留。
- 合并请求必须附带 Bundle 报告（`npm run build:report`，基于 `rollup-plugin-visualizer`），评估依赖体积变化。

---

## 8. 性能优化与监控

### 8.1 前端性能要点
- 路由级代码拆分，列表类页面使用按需加载组件（图表、编辑器等重组件延迟加载）。
- 大数据表格引入虚拟滚动与分页，图片采用懒加载与低分辨率占位。
- 表单校验与搜索请求节流，避免高频触发 API。

### 8.2 静态资源缓存与压缩
- 开启 `build.minify='esbuild'` 保持压缩效率，必要时切换 `terser` 处理兼容性。
- CSS 提取后启用 `postcss-preset-env`，配合 gzip/brotli 压缩；部署侧需配置 `Content-Encoding: br`。
- Service Worker 预留，可在二期接入 `workbox` 缓存关键接口降级数据。

### 8.3 运行监控与性能指标
- 集成前端监控（Sentry / Aliyun ARMS），采集 FP、FCP、LCP、CLS、FID 等核心指标。
- 关键接口响应超过 2s 触发警报，并回传筛选条件、用户 ID 便于溯源。
- 构建阶段产出 `performance-benchmark.md`，记录各版本首屏时间与包体积，支持回归对比。

---

## 9. 安全与隐私保护

### 9.1 认证与授权安全

#### Token 管理
```typescript
// ❌ 不安全：Token 存储在 localStorage
localStorage.setItem('token', token);

// ✅ 安全：使用 httpOnly Cookie（后端设置）
// Set-Cookie: token=xxx; HttpOnly; Secure; SameSite=Strict; Path=/

// ✅ 前端：内存存储 + 刷新机制
const useAuthStore = defineStore('auth', () => {
  const accessToken = ref<string | null>(null);  // 仅内存存储
  const refreshToken = ref<string | null>(null);
  
  // Token 自动刷新
  const refreshAccessToken = async () => {
    // 使用 refreshToken 换取新的 accessToken
  };
});
```

#### 权限控制
```typescript
// 路由守卫 - 权限校验
router.beforeEach(async (to, from, next) => {
  const authStore = useAuthStore();
  
  // 1. 检查是否需要登录
  if (to.meta.requiresAuth && !authStore.isLoggedIn) {
    return next({ name: 'Login', query: { redirect: to.fullPath } });
  }
  
  // 2. 检查角色权限
  if (to.meta.roles && !to.meta.roles.includes(authStore.userRole)) {
    return next({ name: '403' });
  }
  
  // 3. 检查功能权限
  if (to.meta.permissions) {
    const hasPermission = to.meta.permissions.some(p => 
      authStore.permissions.includes(p)
    );
    if (!hasPermission) return next({ name: '403' });
  }
  
  next();
});
```

### 9.2 数据传输安全

#### HTTPS 强制
```typescript
// vite.config.ts - 开发环境启用 HTTPS
export default defineConfig({
  server: {
    https: true,  // 开发环境也使用 HTTPS
  }
});

// Axios 拦截器 - 生产环境强制 HTTPS
axios.interceptors.request.use(config => {
  if (import.meta.env.PROD && config.url?.startsWith('http://')) {
    config.url = config.url.replace('http://', 'https://');
  }
  return config;
});
```

#### 敏感数据加密
```typescript
import CryptoJS from 'crypto-js';

// 敏感字段加密传输
const encryptSensitiveData = (data: string, key: string): string => {
  return CryptoJS.AES.encrypt(data, key).toString();
};

// 登录时密码加密
const loginPayload = {
  username: form.username,
  password: encryptSensitiveData(form.password, PUBLIC_KEY),
  timestamp: Date.now(),  // 防重放攻击
};
```

### 9.3 XSS 防护

#### 输入过滤与输出编码
```typescript
import DOMPurify from 'dompurify';

// 用户输入内容净化
const sanitizeInput = (input: string): string => {
  return DOMPurify.sanitize(input, {
    ALLOWED_TAGS: [],  // 不允许任何 HTML 标签
    ALLOWED_ATTR: [],
  });
};

// 富文本内容净化（允许部分标签）
const sanitizeRichText = (html: string): string => {
  return DOMPurify.sanitize(html, {
    ALLOWED_TAGS: ['p', 'br', 'strong', 'em', 'ul', 'ol', 'li'],
    ALLOWED_ATTR: [],
  });
};

// Vue 模板中避免 v-html
// ❌ 危险
<div v-html="userContent"></div>

// ✅ 安全
<div>{{ userContent }}</div>
<div v-html="sanitizeRichText(userContent)"></div>
```

#### CSP 内容安全策略
```html
<!-- index.html -->
<meta http-equiv="Content-Security-Policy" content="
  default-src 'self';
  script-src 'self' 'unsafe-inline';
  style-src 'self' 'unsafe-inline';
  img-src 'self' data: https:;
  connect-src 'self' https://api.example.com;
  font-src 'self';
  object-src 'none';
  frame-ancestors 'none';
">
```

### 9.4 CSRF 防护

```typescript
// Axios 请求拦截器 - 添加 CSRF Token
axios.interceptors.request.use(config => {
  // 从 Cookie 获取 CSRF Token
  const csrfToken = getCookie('XSRF-TOKEN');
  if (csrfToken) {
    config.headers['X-XSRF-TOKEN'] = csrfToken;
  }
  return config;
});

// 关键操作使用 POST/PUT/DELETE，避免 GET 修改数据
// ❌ 危险
GET /api/admin/user/123/delete

// ✅ 安全
DELETE /api/admin/user/123
```

### 9.5 隐私数据保护

#### 数据脱敏显示
```typescript
// 手机号脱敏
const maskPhone = (phone: string): string => {
  if (!phone || phone.length !== 11) return phone;
  return phone.replace(/(\d{3})\d{4}(\d{4})/, '$1****$2');
};
// 138****1234

// 身份证脱敏
const maskIdCard = (idCard: string): string => {
  if (!idCard || idCard.length < 15) return idCard;
  return idCard.replace(/(\d{4})\d+(\d{4})/, '$1**********$2');
};
// 3301**********1234

// 邮箱脱敏
const maskEmail = (email: string): string => {
  const [name, domain] = email.split('@');
  const maskedName = name.length > 2 
    ? name[0] + '***' + name.slice(-1) 
    : name[0] + '***';
  return `${maskedName}@${domain}`;
};
// z***g@example.com

// OpenID 脱敏
const maskOpenId = (openId: string): string => {
  if (!openId || openId.length < 10) return openId;
  return openId.slice(0, 6) + '****' + openId.slice(-4);
};
```

#### 敏感操作确认
```typescript
// 危险操作二次确认
const handleDelete = async (id: number) => {
  const confirmed = await dialog.warning({
    title: '确认删除',
    content: '删除后数据无法恢复，是否确认删除？',
    positiveText: '确认删除',
    negativeText: '取消',
  });
  
  if (confirmed) {
    // 执行删除
  }
};

// 批量危险操作需要输入确认
const handleBatchDelete = async (ids: number[]) => {
  const input = await dialog.create({
    title: '批量删除确认',
    content: `即将删除 ${ids.length} 条数据，请输入 "确认删除" 继续`,
    // 需要用户输入确认文字
  });
  
  if (input === '确认删除') {
    // 执行批量删除
  }
};
```

### 9.6 日志与审计

#### 操作日志记录
```typescript
// 敏感操作日志 Schema
export const AuditLogSchema = z.object({
  id: z.number(),
  adminId: z.number(),           // 操作管理员
  adminName: z.string(),
  action: z.enum([
    'login', 'logout',           // 认证
    'create', 'update', 'delete', // 数据操作
    'export', 'import',          // 导入导出
    'audit', 'ban', 'unban',     // 审核封禁
  ]),
  module: z.string(),            // 模块：user, works, category
  targetId: z.number().optional(), // 操作对象ID
  targetType: z.string(),        // 对象类型
  oldValue: z.any().optional(),  // 修改前数据
  newValue: z.any().optional(),  // 修改后数据
  ip: z.string(),                // 操作IP
  userAgent: z.string(),         // 浏览器UA
  createdAt: z.string().datetime(),
});

// 前端记录关键操作
const logAction = async (action: string, module: string, data?: any) => {
  await api.post('/api/admin/audit-log', {
    action,
    module,
    ...data,
  });
};
```

### 9.7 防数据泄露策略

#### 导出数据限制
```typescript
// 导出数据限制
const MAX_EXPORT_COUNT = 10000;  // 单次最大导出条数

const handleExport = async () => {
  const { total } = await fetchData();
  
  if (total > MAX_EXPORT_COUNT) {
    message.warning(`单次最多导出 ${MAX_EXPORT_COUNT} 条数据，请缩小筛选范围`);
    return;
  }
  
  // 记录导出日志
  await logAction('export', 'user', { count: total });
  
  // 执行导出
};
```

#### 敏感字段不返回
```typescript
// API 响应拦截 - 移除敏感字段（后端处理，前端兜底）
const removeSensitiveFields = (data: any): any => {
  const sensitiveKeys = ['password', 'salt', 'openid', 'unionid', 'sessionKey'];
  
  if (Array.isArray(data)) {
    return data.map(item => removeSensitiveFields(item));
  }
  
  if (data && typeof data === 'object') {
    const cleaned = { ...data };
    sensitiveKeys.forEach(key => delete cleaned[key]);
    return cleaned;
  }
  
  return data;
};
```

#### 请求频率限制
```typescript
// 防暴力破解 - 登录限制
const loginAttempts = new Map<string, { count: number; lastAttempt: number }>();

const checkLoginLimit = (username: string): boolean => {
  const record = loginAttempts.get(username);
  const now = Date.now();
  
  if (record) {
    // 5分钟内超过5次失败
    if (now - record.lastAttempt < 5 * 60 * 1000 && record.count >= 5) {
      return false;  // 禁止登录
    }
  }
  
  return true;
};
```

### 9.8 安全检查清单

| 类别 | 检查项 | 状态 |
|------|--------|------|
| **认证** | Token 使用 httpOnly Cookie | ☐ |
| | 密码加密传输 | ☐ |
| | 登录失败次数限制 | ☐ |
| | 会话超时自动登出 | ☐ |
| **权限** | 路由权限守卫 | ☐ |
| | API 权限校验 | ☐ |
| | 功能级别权限控制 | ☐ |
| **XSS** | 用户输入过滤 | ☐ |
| | 输出编码 | ☐ |
| | CSP 策略配置 | ☐ |
| **CSRF** | CSRF Token 校验 | ☐ |
| | SameSite Cookie | ☐ |
| **隐私** | 敏感数据脱敏显示 | ☐ |
| | 导出数据限制 | ☐ |
| | 操作日志记录 | ☐ |
| **传输** | 全站 HTTPS | ☐ |
| | 敏感数据加密 | ☐ |

---

## 10. 测试与发布流程

### 10.1 测试分层
| 阶段 | 目标 | 覆盖范围 | 工具 |
|------|------|----------|------|
| 单元测试 | 校验纯函数与 Pinia Store 逻辑 | `utils/`, `composables/`, `store/` | Vitest + @vue/test-utils |
| 组件测试 | 验证核心组件交互与渲染 | 表格、表单、图表组件 | Vitest Component Testing |
| 集成测试 | 确保路由与数据流完整性 | 登录、Dashboard、审核流程 | Cypress |
| 接口联调 | 校验 API 协议与异常处理 | `/api/admin/**` 核心接口 | MSW / Postman |
| 回归测试 | 发布前覆盖关键业务路径 | 核心 CRUD、权限、导出流程 | Cypress + 手工 Checklist |

### 10.2 提交流程门禁
- PR 必须附带 `npm run lint`、`npm run typecheck`、`npm run test -- --run` 结果截图或流水线报告。
- 若改动影响打包体积，需追加 `npm run build:report` 截图并在 MR 描述中说明体积变化原因。
- 设计稿/原型变更后同步更新 Storybook 或截图附件，避免 UI 回归遗漏。

### 10.3 发布与回滚策略
- 流水线：`lint → unit/component test → build → e2e smoke → 上传产物 → 自动部署`，任一步失败需阻断发布。
- 生产发布采用蓝绿发布或灰度方案，灰度窗口≥30 分钟，监控指标无异常后再全量放量。
- 部署成功后执行管理员端冒烟脚本（登录、分类 CRUD、审核动作、导出）确认；异常时回滚至上一个版本目录并恢复缓存。
- 所有发布记录（版本号、Git 提交、负责人、发布时间）登记在 Release Log，便于审计追踪。

---

## 📝 注意事项

1. **代码规范**
   - 组件使用 `<script setup>` 语法
   - 使用 Composition API
   - 类型定义完整，避免 any
   - 组件命名采用 PascalCase

2. **接口对接**
   - 统一使用 request.ts 封装的方法
   - 接口错误统一处理
   - 请求携带 Token

3. **性能优化**
   - 路由懒加载
   - 图片懒加载
   - 大列表虚拟滚动
   - 合理使用缓存

4. **安全考虑**
   - Token 存储使用 httpOnly Cookie
   - 敏感操作二次确认
   - 防止 XSS 攻击
   - 接口权限校验
   - 敏感数据脱敏处理
   - 操作日志审计追踪

---

> 📌 本文档将随项目开发持续更新
