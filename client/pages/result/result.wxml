<view class="container result-container">
  <!-- 主图预览 -->
  <view class="main-image-section fade-in">
    <view class="main-image-wrapper" bindtap="previewImage" data-index="0">
      <image 
        class="main-image" 
        src="{{mainImage}}" 
        mode="aspectFill"
        show-menu-by-longpress
      />
      <view class="image-badge">
        <text>{{currentIndex + 1}}/{{images.length}}</text>
      </view>
    </view>
  </view>

  <!-- 候选图 -->
  <view class="thumbs-section fade-in" style="animation-delay: 0.1s;" wx:if="{{images.length > 1}}">
    <text class="section-title" style="margin-top: 0;">候选图</text>
    <scroll-view scroll-x class="thumbs-scroll" enhanced show-scrollbar="{{false}}">
      <view class="thumbs-list">
        <block wx:for="{{images}}" wx:key="index">
          <view 
            class="thumb-item {{currentIndex === index ? 'active' : ''}}" 
            bindtap="selectImage" 
            data-index="{{index}}"
          >
            <image class="thumb-image" src="{{item}}" mode="aspectFill" />
          </view>
        </block>
      </view>
    </scroll-view>
  </view>

  <!-- 文案区域 -->
  <view class="copy-section fade-in" style="animation-delay: 0.15s;">
    <view class="section-header">
      <text class="section-title" style="margin: 0;">文案</text>
      <view class="copy-all-btn" bindtap="copyAllText">
        <text class="copy-icon">📋</text>
        <text>一键复制</text>
      </view>
    </view>
    
    <view class="card copy-card">
      <view class="copy-item">
        <text class="copy-label">标题</text>
        <text class="copy-value" bindtap="copyText" data-type="title">{{copyText.title}}</text>
      </view>
      <view class="copy-item">
        <text class="copy-label">摘要</text>
        <text class="copy-value" bindtap="copyText" data-type="summary">{{copyText.summary}}</text>
      </view>
      <view class="copy-item">
        <text class="copy-label">标签</text>
        <view class="tags-wrapper">
          <block wx:for="{{copyText.tags}}" wx:key="index">
            <view class="tag" bindtap="copyText" data-type="tag" data-value="{{item}}">{{item}}</view>
          </block>
        </view>
      </view>
    </view>
  </view>

  <!-- 操作按钮 -->
  <view class="action-section fade-in safe-bottom" style="animation-delay: 0.2s;">
    <view class="action-row">
      <view class="btn-ghost action-btn" bindtap="regenerate">
        <text class="btn-icon">🔄</text>
        <text>再次生成</text>
      </view>
      <view class="btn-primary action-btn" bindtap="downloadImage">
        <text class="btn-icon">💾</text>
        <text>保存图片</text>
      </view>
    </view>
    <view class="action-row mt-md">
      <view class="btn-secondary action-btn full" bindtap="hdGenerate">
        <text class="btn-icon">✨</text>
        <text>高清生成</text>
        <view class="hd-badge">HD</view>
      </view>
    </view>
  </view>
</view>
